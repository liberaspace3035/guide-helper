[phases.setup]
nixPkgs = { php = "php83", nodejs = "nodejs_20" }

[phases.install]
cmds = [
    "composer install --no-dev --optimize-autoloader",
    "npm ci --omit=dev"
]

[phases.build]
cmds = [
    "php artisan config:cache",
    "php artisan route:cache",
    "php artisan view:cache"
]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"

